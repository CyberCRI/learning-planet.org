---
import BaseLayout from '~/layouts/BaseLayout.astro';
import LegalPageContent from '~/components/LegalPageContent.astro';
import { getLegalPage } from '~/data/legal';

const locale = 'fr';
const page = getLegalPage('privacy-policy', locale);
---
<BaseLayout title={page.title} description={page.metaDescription} path="privacy-policy" locale={locale}>
  <LegalPageContent page={page} locale={locale} />
</BaseLayout>

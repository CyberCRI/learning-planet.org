---
import BaseLayout from '~/layouts/BaseLayout.astro';
import LegalPageContent from '~/components/LegalPageContent.astro';
import { getLegalPage } from '~/data/legal';

const locale = 'fr';
const page = getLegalPage('cookies-policy', locale);
---
<BaseLayout title={page.title} description={page.metaDescription} path="cookies-policy" locale={locale}>
  <LegalPageContent page={page} locale={locale} showManageCookies />
</BaseLayout>
